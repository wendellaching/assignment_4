var myCanvas;

function setup() {
	// create a place to draw
	createCanvas(640, 480);
	background(90, 90, 110);
	noStroke();
	noLoop();
}

function draw() {
	// clear the background
	fill(0, 0,0);
	rect(40,50,250,380);
	myCanvas = (320,480);



}
